import * as React from 'react';
import { Box } from '@mui/material';

const HomeView = (props) => {
    return (
        <Box>
            {"homeview"}
        </Box>
    )
}

export default HomeView;